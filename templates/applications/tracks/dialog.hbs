<form class="empyrean flexcol spaced">
  <div class="form-group">
    <label>{{localize "empyrean.TRACKS.label"}}</label>
    <div class="form-fields">
      <input type="text" id="label" value="{{label}}" placeholder="{{localize "empyrean.TRACKS.label"}}" />
    </div>
  </div>

  <div class="form-group">
    <label>
      {{localize "empyrean.TRACKS.trackGroups"}}
    </label>
    <div class="form-fields">
      <input type="text" id="groups" value="{{groups}}" placeholder="{{localize "empyrean.TRACKS.trackGroups"}}" />
      <i class="fas fa-question-circle flex0" data-tooltip="{{localize "empyrean.TRACKS.trackGroupsHelpText"}}"></i>
    </div>
  </div>

  <div class="form-group">
    <label>{{localize "empyrean.TRACKS.visibility"}}</label>
    <div class="form-fields">
      <select name="visibility" id="visibility">
        {{selectOptions @root.config.trackVisibilityOptions selected=visibility localize=true}}
      </select>
    </div>
  </div>
</form>
